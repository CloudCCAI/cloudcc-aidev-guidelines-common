# Environment variable names for {{ENVIRONMENT}}.
# Record example or non-secret values only. Never place credentials or tokens here.
# Add confirmed variables below, for example: APPLICATION_PORT=8080
